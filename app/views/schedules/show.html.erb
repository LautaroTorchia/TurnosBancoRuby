<div class="jumbotron">
  <h1 class="display-4">
    Dia: <%= @schedule.day %>
  </h1>
  <p class="lead">
    Abre a las: <%= @schedule.open_at.strftime("%H:%M:%S") %>  | 
    Cierra a las: <%= @schedule.close_at.strftime("%H:%M:%S") %>  
  </p>
  <br/>
<div>

<div>
  <div class='d-flex'>
  <% if can? :update, @schedule %>
    <%= link_to "Editar Horario", edit_branch_schedule_path(@schedule),class: "btn btn-secondary" %> 
  <% end %>
  <% if can? :destroy, @schedule %>
    <%= button_to "Eliminar Horario",branch_schedule_url(@branch,@schedule), method: :delete,class: "btn btn-danger" %> 
  <% end %>
  <%= link_to "Volver al listado", branch_schedules_path,class: "btn btn-dark"%>
  </div>

</div>
